services:
  - type: web
    name: video-app-finder
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
